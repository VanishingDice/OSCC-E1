# Vim 常用命令速查表（A4 打印版）

> 说明：`[C]` 表示 Ctrl 键，如 `[C]` 即 Ctrl。  
> 普通模式下命令区分大小写，小写与大写功能不同。

---

## 1. 模式切换
| 命令 | 说明 |
|------|------|
| `Esc` / `[C][` | 返回普通模式（常用） |
| `i` | 光标前插入 |
| `I` | 行首插入 |
| `a` | 光标后插入 |
| `A` | 行尾插入 |
| `o` | 下方新建行插入 |
| `O` | 上方新建行插入 |
| `v` | 可视（字符） |
| `V` | 可视行 |
| `[C]v` | 可视块 |
| `R` | 替换模式（覆盖） |

---

## 2. 光标移动（核心）
| 命令 | 说明 |
|------|------|
| `h` `j` `k` `l` | 左 下 上 右（禁用方向键） |
| `w` | 下词开头 |
| `b` | 上词开头 |
| `e` | 词尾 |
| `0` | 行首（绝对） |
| `^` | 行首非空 |
| `$` | 行尾 |
| `gg` | 文件首 |
| `G` | 文件尾 |
| `:n` | 跳转到第 n 行 |
| `[C]u` / `[C]d` | 上/下翻半页 |
| `[C]f` / `[C]b` | 上/下翻一页 |
| `%` | 跳转匹配括号 |

---

## 3. 删除 / 复制 / 粘贴（公式：动词+移动）
### 动词
| 命令 | 说明 |
|------|------|
| `d` | 删除（剪切） |
| `y` | 复制（yank） |
| `c` | 修改（删+插入） |
| `p` / `P` | 后/前粘贴 |
| `x` | 删当前字符 |
| `X` | 删前字符 |

### 常用组合
| 组合 | 说明 |
|------|------|
| `dw` | 删到下词首 |
| `de` | 删到词尾 |
| `d$` / `D` | 删到行尾 |
| `d0` | 删到行首 |
| `dd` | 删整行 |
| `yy` | 复制整行 |
| `yw` | 复制词 |
| `y$` | 复制到行尾 |
| `cc` | 改整行 |
| `cw` | 改词 |
| `C` | 改到行尾 |
| `s` | 删字符并插入 |
| `S` | 删整行并插入 |

### 文本对象（`i` 内部 / `a` 周围）
| 组合 | 说明 |
|------|------|
| `diw` / `ciw` | 删/改当前词 |
| `di"` / `ci"` | 删/改双引号内 |
| `da"` / `ca"` | 删/改双引号及内容 |
| `di(` / `ci(` | 删/改括号内 |
| `dit` | 删 HTML 标签内 |

---

## 4. 搜索与替换
| 命令 | 说明 |
|------|------|
| `/pattern` | 向下搜索 |
| `?pattern` | 向上搜索 |
| `n` / `N` | 下一个/上一个 |
| `*` / `#` | 向下/上搜索当前词 |
| `:s/old/new/` | 当前行第一个替换 |
| `:s/old/new/g` | 当前行全部替换 |
| `:%s/old/new/g` | 全文件替换 |
| `:%s/old/new/gc` | 全文件逐个确认 |
| `:n,m s/old/new/g` | 第 n~m 行替换 |

---

## 5. 文件 / 窗口 / 标签
| 命令 | 说明 |
|------|------|
| `:w` | 保存 |
| `:w filename` | 另存为 |
| `:q` | 退出 |
| `:q!` | 强制退出（不保存） |
| `:wq` / `:x` / `ZZ` | 保存并退出 |
| `:e filename` | 打开文件 |
| `:e!` | 重载当前文件 |
| `:sp` / `:vs` | 水平/垂直分屏 |
| `[C]w h/j/k/l` | 分屏间切换 |
| `[C]w q` | 关闭分屏 |
| `:tabnew` | 新建标签页 |
| `gt` / `gT` | 下一/上一标签 |

---

## 6. 撤销 / 重做 / 重复
| 命令 | 说明 |
|------|------|
| `u` | 撤销 |
| `[C]r` | 重做 |
| `.` | 重复上次修改（神器） |

---

## 7. 可视模式操作
| 命令 | 说明 |
|------|------|
| `v` / `V` / `[C]v` | 进入可视模式 |
| 选中后 `d` `y` `c` | 删/复制/修改 |
| `>` / `<` | 缩进/反缩进 |
| `=` | 自动缩进 |

---

## 8. 宏录制
| 命令 | 说明 |
|------|------|
| `q{字母}` | 开始录制到寄存器（如 `qa`） |
| `q` | 停止录制 |
| `@{字母}` | 回放（如 `@a`） |
| `@@` | 重复上次回放 |

---

## 9. 缓冲区与跳转
| 命令 | 说明 |
|------|------|
| `:ls` / `:buffers` | 列出所有缓冲区 |
| `:b n` / `:b name` | 切换到第 n 个或文件名 |
| `:bd` | 删除当前缓冲区 |
| `[C]o` / `[C]i` | 后退/前进光标位置 |

---

## 10. 常用设置（`~/.vimrc`）
| 配置 | 说明 |
|------|------|
| `set number` / `set nu` | 显示行号 |
| `set relativenumber` / `set rnu` | 相对行号 |
| `set expandtab` / `set et` | 空格代替 Tab |
| `set tabstop=4` / `set ts=4` | Tab 宽度 4 |
| `set shiftwidth=4` / `set sw=4` | 缩进宽度 4 |
| `set autoindent` / `set ai` | 自动缩进 |
| `set hlsearch` | 高亮搜索结果 |
| `set incsearch` | 实时搜索高亮 |
| `set ignorecase` / `set ic` | 搜索忽略大小写 |
| `set smartcase` | 智能大小写 |
| `syntax on` | 语法高亮 |
| `set mouse=a` | 启用鼠标 |

---

## 11. 帮助
| 命令 | 说明 |
|------|------|
| `:help` / `:h` | 打开帮助 |
| `:h command` | 查看命令帮助 |

---

> **打印小贴士**：复制到 Word 后，使用“分栏”功能（2 栏），并缩小字号至 9pt，可将其压缩到 2 张 A4 纸内。